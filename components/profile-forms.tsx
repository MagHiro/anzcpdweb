"use client";

import { useState } from "react";
import { Field, FormNotice, TextInput } from "@/components/ui";

export function ProfileForm({ name, email }: { name: string; email: string }) { const [state, setState] = useState<{ error?: string; success?: string }>({}); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const payload = Object.fromEntries(new FormData(event.currentTarget).entries()); const response = await fetch("/api/account/profile", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }); const result = await response.json(); setState(response.ok ? { success: "Profile updated." } : { error: result.error ?? "Profile could not be updated." }); } return <form onSubmit={submit} className="space-y-5"><FormNotice>{state.error}</FormNotice><FormNotice tone="success">{state.success}</FormNotice><Field label="Name" required><TextInput name="name" defaultValue={name} required /></Field><Field label="Email address"><TextInput value={email} readOnly className="bg-[#f2f5f3]" /></Field><button className="focus-ring min-h-11 rounded-full bg-[var(--forest)] px-5 text-sm font-bold text-white" type="submit">Save profile</button></form>; }

export function ChangePasswordForm() { const [state, setState] = useState<{ error?: string; success?: string }>({}); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const payload = Object.fromEntries(new FormData(event.currentTarget).entries()); const response = await fetch("/api/account/change-password", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }); const result = await response.json(); setState(response.ok ? { success: "Password updated. Other sessions can be revoked from your auth provider controls." } : { error: result.error ?? "Password could not be updated." }); } return <form onSubmit={submit} className="space-y-5"><FormNotice>{state.error}</FormNotice><FormNotice tone="success">{state.success}</FormNotice><Field label="Current password" required><TextInput name="currentPassword" type="password" autoComplete="current-password" required /></Field><Field label="New password" hint="Use at least 12 characters." required><TextInput name="newPassword" type="password" autoComplete="new-password" minLength={12} required /></Field><button className="focus-ring min-h-11 rounded-full bg-[var(--forest)] px-5 text-sm font-bold text-white" type="submit">Update password</button></form>; }
