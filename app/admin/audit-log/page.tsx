import { getAuditLogRows } from "@/server/catalogue/queries";

export default async function AdminAuditLogPage() { const rows = await getAuditLogRows(); return <div><p className="eyebrow text-[var(--fern)]">Traceability</p><h1 className="display mt-3 text-5xl text-[var(--forest)]">Audit log</h1><p className="mt-3 text-sm text-[var(--muted)]">Administrative changes and financial actions. Use the change details to review the context of each event.</p><div className="mt-8 grid gap-3">{!rows.length && <p className="rounded-xl border border-[var(--line)] bg-white p-8 text-sm text-[var(--muted)]">No audit events to display.</p>}{rows.map((row) => <div key={row.audit.id} className="rounded-[1.2rem] border border-[var(--line)] bg-white p-5"><div className="flex flex-wrap justify-between gap-3"><p className="text-sm font-bold text-[var(--forest)]">{row.audit.action.replaceAll("_", " ")}</p><time className="text-xs text-[var(--muted)]">{new Intl.DateTimeFormat("en-AU", { dateStyle: "medium", timeStyle: "short" }).format(row.audit.createdAt)}</time></div><p className="mt-2 text-xs text-[var(--muted)]">{row.actorName ?? "System"} · {row.audit.entityType} · {row.audit.entityId}</p>{row.audit.metadata ? <details className="mt-4"><summary className="cursor-pointer text-xs font-semibold text-[var(--forest)]">View change details</summary><pre className="mt-3 overflow-x-auto rounded-lg bg-[#f4f6f4] p-3 text-xs text-[var(--muted)]">{JSON.stringify(row.audit.metadata, null, 2)}</pre></details> : null}</div>)}</div></div>; }
