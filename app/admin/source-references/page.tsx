import { getAdminSourceReferences } from "@/server/catalogue/queries";
import { SourceReferenceForm } from "@/components/admin-forms";
import { StatusBadge, LinkButton } from "@/components/ui";

export default async function AdminSourceReferencesPage() {
  const rows = await getAdminSourceReferences();
  return <div><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="eyebrow text-[var(--fern)]">Editorial traceability</p><h1 className="display mt-3 text-5xl text-[var(--forest)]">Source references</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[var(--muted)]">Maintain the official sources used to check course terminology and attach them to classes. A reference is not an accreditation claim.</p></div><LinkButton href="#new-source">New reference</LinkButton></div><div className="mt-8 overflow-x-auto rounded-[1.3rem] border border-[var(--line)] bg-white"><table className="w-full min-w-[800px] text-left text-sm"><thead className="border-b border-[var(--line)] text-xs uppercase tracking-[.1em] text-[var(--muted)]"><tr><th className="px-5 py-4">Source</th><th className="px-5 py-4">Jurisdiction</th><th className="px-5 py-4">Checked</th><th className="px-5 py-4">Edit</th></tr></thead><tbody className="divide-y divide-[var(--line)]">{rows.map((source) => <tr key={source.id}><td className="px-5 py-4"><a href={source.url} target="_blank" rel="noreferrer" className="font-bold text-[var(--forest)] underline underline-offset-4">{source.title}</a><span className="block text-xs text-[var(--muted)]">{source.authority}</span></td><td className="px-5 py-4"><StatusBadge status={source.jurisdiction === "AU" ? "AUSTRALIA" : "NEW ZEALAND"} /></td><td className="px-5 py-4 text-xs text-[var(--muted)]">{new Intl.DateTimeFormat("en-AU", { dateStyle: "medium", timeZone: "UTC" }).format(source.checkedAt)}</td><td className="px-5 py-4"><a href={`#edit-${source.id}`} className="font-bold text-[var(--forest)] underline underline-offset-4">Edit</a></td></tr>)}</tbody></table>{!rows.length ? <p className="p-10 text-center text-sm text-[var(--muted)]">No source references yet.</p> : null}</div><div id="new-source" className="mt-8 rounded-[1.3rem] border border-[var(--line)] bg-white p-6"><p className="eyebrow text-[var(--fern)]">Create</p><h2 className="display mt-2 text-3xl text-[var(--forest)]">Add an official reference</h2><div className="mt-5"><SourceReferenceForm /></div></div><div className="mt-8 grid gap-8">{rows.map((source) => <div id={`edit-${source.id}`} key={`edit-${source.id}`} className="scroll-mt-6 rounded-[1.3rem] border border-[var(--line)] bg-white p-6"><p className="eyebrow text-[var(--muted)]">Edit reference</p><h2 className="display mt-2 text-3xl text-[var(--forest)]">{source.title}</h2><div className="mt-5"><SourceReferenceForm source={source} /></div></div>)}</div></div>;
}
